// Stub: PreviewRenderer neutralized (blocks architecture removed during cleanup)
import React from 'react';

interface PreviewRendererProps {
  block?: any;
}

export function PreviewRenderer({ block }: PreviewRendererProps) {
  return <div></div>;
}